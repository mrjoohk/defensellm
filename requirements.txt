# Defense Domain sLLM/Agent System — Dependencies
# Python 3.10+

# Core runtime
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
httpx>=0.27.0             # FastAPI TestClient (Phase 5)

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
python-multipart>=0.0.22
numpy>=1.26.0

# ---------------------------------------------------------------------------
# P0 production dependencies (now active)
# ---------------------------------------------------------------------------
PyJWT>=2.8.0              # JWT authentication (security/auth.py)
transformers>=4.41.0      # Qwen2.5 model loading (serving/qwen_adapter.py, rag/embedder.py)
accelerate>=0.30.0        # device_map="auto" for GPU placement

# ---------------------------------------------------------------------------
# PDF ingestion + OCR (UF-025)
# ---------------------------------------------------------------------------
# opendataloader-pdf extracts text from digital PDFs using an embedded Java JAR.
# pytesseract + pdf2image provide Tesseract-based OCR for image/scanned PDFs.
#
# System prerequisites (install separately, NOT via pip):
#   Java 11+      : https://adoptium.net/  (required by opendataloader-pdf)
#   Tesseract-OCR : https://github.com/UB-Mannheim/tesseract/wiki      (Windows)
#                   apt install tesseract-ocr tesseract-ocr-kor         (Linux)
#   Poppler       : https://github.com/oschwartz10612/poppler-windows/releases/ (Windows)
#                   apt install poppler-utils                           (Linux)
opendataloader-pdf>=2.0.2     # PDF text extraction (Java engine, offline)
pytesseract>=0.3.13           # OCR fallback for image-based / scanned PDFs
pdf2image>=1.17.0             # PDF → PIL image conversion (requires poppler)
Pillow>=10.0.0                # Image processing (required by pytesseract)

# ---------------------------------------------------------------------------
# Optional / future production dependencies
# ---------------------------------------------------------------------------
# torch>=2.0.0              # installed via conda (conda env: defensellm)
# faiss-cpu>=1.8.0          # production ANN vector index (todo.md P1)
# rank-bm25>=0.2.2          # production BM25 (todo.md P1)
# vllm>=0.4.0               # high-throughput serving adapter (todo.md P0 — vLLM path)
# psycopg2-binary>=2.9.9    # Postgres backend (todo.md P1)
# PyMuPDF>=1.24.0           # alternative PDF parser (superseded by opendataloader-pdf)
